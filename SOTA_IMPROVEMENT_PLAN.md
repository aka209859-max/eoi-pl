競馬予想における特徴量管理方式の最新動向とEOI-PLハイブリッド方式の競争力評価（2023-2026）エグゼクティブサマリー本報告書は、2023年から2026年初頭にかけての競馬予想AIおよびスポーツベッティングにおける技術的景観を背景に、提案された「EOI-PL（Plackett-Luce + Power EP）」モデルおよびその特徴量管理方式（Global 95% + Context-Specific 5%）の競争力を包括的に評価したものである。分析の結果、EOI-PLが採用するPlackett-Luce（PL）ランキングモデルと**Power Expectation Propagation（Power EP）**の組み合わせは、不確実性を伴う多頭数競走のモデリングにおいて、理論的堅牢性と計算効率のバランスが取れた高度なアプローチであることが確認された。特に、Top3ランク内に少なくとも1頭の入賞馬を含む確率（$Top3 \ge 1$）が90.06%という数値は、ベースラインとなる統計モデルや一般的な商用AIのパフォーマンスを凌駕しており、馬券戦略における「軸馬選定」の信頼性が極めて高いことを示唆している。一方で、「全競馬場混合データ（Global Features）95%」対「競馬場別適応度データ（Context-Specific Features）5%」という固定的な重み配分は、2026年時点のState-of-the-Art（SOTA）と比較して顕著な脆弱性を孕んでいる。近年のトレンドは、TransformerやAttention機構を用いた動的なコンテキスト重み付けや、**エンティティ埋め込み（Entity Embeddings）**による適応度の自動学習へとシフトしており、物理的なトラックバイアスや展開の非線形性を捉える上で、EOI-PLの静的な5%配分は「過小評価」であるとの結論に至った。本レポートでは、EOI-PLの数理的構造の深層分析に加え、最新のDeep Learning手法、勾配ブースティング決定木（GBDT）、および商用システム（AlphaImpact、Netkeiba等）との比較を行い、EOI-PLを次世代の支配的な予測システムへと進化させるための具体的な改善ロードマップを提示する。1. 競馬予想AIにおける特徴量エンジニアリングの変遷と最新動向（2023-2026）2023年から2026年にかけて、競馬予想およびスポーツベッティングのアルゴリズムは、単なる「着順予測」から、レース全体のダイナミクスを解明する「ランキング学習（Learning to Rank）」および「生成型シミュレーション」へとパラダイムシフトを遂げた。かつて主流であった、走破タイムを目的変数とする回帰モデルや、1着/着外を分類する単純な分類モデルは、レースごとの相対的な力関係を捉えきれないという限界から、第一線を退きつつある。1.1 ランキング学習（Learning-to-Rank）への収束現代の競馬予測において最も顕著なトレンドは、予測タスクを「ランキング問題」として再定義する動きである。競馬は絶対的なタイムを競うものではなく、そのレースに参加したメンバーの中での相対的な順位によって配当が決定される。したがって、モデルの目的関数は、全順列の発生確率を最大化するものでなければならない。この文脈において、リストワイズ（Listwise）学習のアプローチが2024年以降のデファクトスタンダードとなっている 。従来のペアワイズ（Pairwise）手法（例：RankNet）が「馬Aが馬Bより先着するか」という局所的な関係のみを学習していたのに対し、リストワイズ手法（例：ListMLE, ListNet）はレース全体のメンバー構成を考慮し、トップランクの順序正解率を直接的に最適化する。2025年の主要なカンファレンスや実装事例においても、損失関数としてListMLEや、その派生形であるPolyLoss、LambdaLossを採用する事例が増加しており、EOI-PLがListMLEを採用している点は、現代的な最適化トレンドと完全に合致していると言える 。1.2 「Global」対「Context-Specific」のハイブリッドアーキテクチャ特徴量管理の観点からは、汎用的な能力（Global）と環境特異的な適応度（Context-Specific）をいかに統合するかが主要な研究テーマとなっている。Global Features（全体的特徴量）: 馬の潜在能力（ポテンシャル）、騎手の通算成績、血統データなど、レース条件に依存しない不変的な属性。これらは「ベースレート」を形成し、特にデータが少ない新馬や未勝利戦において予測のアンカーとして機能する 。Context-Specific Features（文脈的特徴量）: 特定の競馬場、距離、馬場状態（良・稍重・重・不良）、天候におけるパフォーマンス。近年の研究では、これを単なるカテゴリカル変数として扱うのではなく、**交差特徴量（Cross Features）や埋め込みベクトル（Embeddings）**として表現する手法が主流である 。Q1-1およびQ1-2に対する回答として、GlobalとContext-Specificを組み合わせるハイブリッド方式は、2023-2026年のスポーツベッティングAIにおいて極めて一般的かつ必須のアプローチである。しかし、EOI-PLのように「95:5」という極端かつ固定的な比率で管理する事例は稀有であり、現代のSOTAモデルでは、Deep LearningのAttention機構やGBDTの分割探索を通じて、レースごとに最適な重み配分を動的に決定するアーキテクチャが採用されている 。1.3 コールドスタート問題への対処新規の競走馬（新馬）や、地方競馬（NAR）から中央競馬（JRA）へ転入してきた馬など、当該環境でのデータが存在しない「コールドスタート問題」は依然として大きな課題である。
これに対し、2026年時点での有効な解決策として、転移学習（Transfer Learning）と階層ベイズモデルの適用が進んでいる。EOI-PLが採用する「全競馬場混合データ（Global Features）」を95%の重みで利用する戦略は、データ希薄性に対する強力な正則化として機能するため、コールドスタート対策としては理にかなっている 。特に、Power EPのようなベイズ推論を用いることで、データがない馬に対しては「平均的な馬の事前分布」を割り当て、レースを重ねるごとに事後分布を更新していくアプローチが可能となる。2. EOI-PL 特徴量管理方式（95:5配分）の深層分析EOI-PL方式の最大の特徴かつ議論の余地がある点は、Global Featuresに95%、Context-Specific Featuresにわずか5%という極端な重み付けを行っている点にある。2.1 重み配分（95:5）の妥当性評価数理的な観点およびドメイン知識の観点から、この配分には再考の余地がある。Global過信のリスク: 95%という重みは、「馬の能力は環境に左右されず一定である」という強い仮定（Strong Prior）を置いているに等しい。確かに、G1クラスのトップホースであれば、どのような競馬場でも能力を発揮するかもしれないが、下級条件戦やハンデ戦においては、コース適性（右回り・左回り、坂の有無、直線の長さ）が勝敗を分ける決定的な要因となることが多い 。5%の限界: Context-Specific Features（トラック適応度）への配分が5%に制限されている場合、例えば「中山競馬場（急坂・小回り）では無類の強さを発揮するが、東京競馬場（長い直線）では惨敗する」といったコーススペシャリストの検出が困難になる。文献や実装事例によれば、特定のトラックバイアスが存在する場合、適性の重みは30%〜40%程度まで上昇すべき状況も確認されており、5%のキャップは予測精度の上限を不必要に低くしている可能性が高い 。2.2 トラックバイアス（Track Bias）のモデリングQ2-4に関連する最新動向として、トラックバイアスのモデリングは、単なる「コースごとの平均着順」という静的な統計から、物理的・時系列的なバイアス検知へと進化している。物理的バイアス: 内ラチ沿いの芝の痛み具合、散水作業による含水率のムラなどを、高解像度の馬場画像やGPSトラッキングデータから推定する試みが行われている 。展開バイアス: 「逃げ有利」「差し有利」といった展開の傾向は、風向きや騎手の心理状態、当日の馬場コンディションによって刻一刻と変化する。
EOI-PLの定義にある「track_adaptation[horse_id][track_code] = {avg_rank}」という単純なルックアップテーブル方式では、これらの動的なバイアス（今日の馬場は内が伸びる、等）を反映できない。現代のモデルでは、レース当日の直近数レースの結果（トラックバイアス係数）をリアルタイムでフィードバックし、Context特徴量の重みを動的に引き上げる処理が一般的である 。2.3 特徴量相互作用の欠落Global（30%+15%+...）とContext（5%）を線形に加算する方式では、特徴量間の非線形な相互作用を捉えられない。例えば、「大型馬（Global）」$\times$「道悪（Context）」$\times$「パワー型騎手（Global）」という組み合わせが、単独の要素の和以上のパフォーマンスを発揮するケースは頻繁に見られる。Deep LearningやGBDT（LightGBM/XGBoost）が優れているのは、こうした相互作用を自動的に学習する点にある。EOI-PLの設計は、解釈性は高いものの、表現力において劣後していると言わざるを得ない。3. アルゴリズム評価：Plackett-Luce + Power EP + ListMLEEOI-PLの核となる数理モデルは、確率的ランキングモデルの王道を行く構成であり、学術的な評価は極めて高い。3.1 Plackett-Luce (PL) モデルの有効性Q4-1に対し、PLモデルは競馬予想において極めて有効であると断言できる。多頭数（最大18頭）が競うレースにおいて、任意の順列（1着から18着までの並び）が発生する確率 $P(\pi)$ を、各馬の潜在的な「強さ」パラメータ $\theta$ を用いて以下のようにモデル化する。$$P(\pi|\theta) = \prod_{i=1}^{M} \frac{e^{\theta_{\pi_i}}}{\sum_{j=i}^{M} e^{\theta_{\pi_j}}}$$このモデルは、ニューラルネットワーク（RankNet等）やGBDT（LambdaMART）の目的関数としても組み込むことが可能であり、特に「オッズ（勝率）」と「順位」の整合性を取る上で、回帰モデルよりも優れた特性を持つ 。LightGBM等のGBDTは「決定木」ベースであるため、特徴量の非線形性を捉える能力は高いが、ランキング全体の確率分布を出力する能力においてはPLモデル（およびその拡張）が勝る。3.2 Power EP (Expectation Propagation) の競争力Q4-2に関して、Power EP（$\alpha=0.5$）の採用は、計算コストと推定精度のトレードオフにおいて最適な選択の一つである。vs MCMC: MCMCは事後分布の推定において最も正確だが、数万レース規模のデータセットに対して反復計算を行うには計算コストが高すぎる。リアルタイム性が求められるオッズ分析や、頻繁なモデル更新が必要な環境ではPower EPの実用性が勝る 。vs 変分推論 (VI): 一般的な変分推論（KLダイバージェンス最小化）は、事後分布の分散を過小評価する傾向がある（不確実性を甘く見積もる）。これは、リスク管理が重要なギャンブルにおいて致命的になり得る。Power EPは $\alpha$-ダイバージェンスを用いることで、分布の裾野（テイル）を適切に評価し、よりロバストな不確実性推定が可能である 。$\alpha=0.5$ の妥当性: $\alpha=0.5$（Hellinger距離に対応）は、分布のモード（最頻値）と広がり（分散）のバランスを取る設定として経験的に優れていることが知られている。外れ値（予期せぬ大穴や落馬）の影響を緩和しつつ、安定したパラメータ推定を実現する設定である。3.3 ListMLEによる学習ListMLEは、正解の順位列（Ground Truth）の対数尤度を最大化する手法であり、PLモデルのパラメータ推定において最も直接的かつ強力なアプローチである。
Q4-3に対し、競馬予想においてはPairwise（ペアワイズ）よりもListwise（ListMLE）が最適であるとの見解が支持される。ペアワイズ法は「1着と2着の差」も「17着と18着の差」も等しく扱ってしまう傾向があるが、競馬においては「上位（馬券圏内）の順序」が極めて重要であり、下位の順序はどうでもよい。ListMLE（特にTop-kに重みを置いた派生形）は、上位の並び替えに対する感度が高く、馬券戦略に直結する精度を向上させやすい 。4. 予測精度の定量的評価とベンチマーク提示された精度指標（Top3 $\ge$ 1: 90.06%, Top5 $\ge$ 3: 28.23%）について、業界標準との比較を行う。4.1 Top3 $\ge$ 1 (90.06%) の評価この指標は、「モデルが予測した上位3頭（推奨馬）の中に、実際の1着〜3着馬（入賞馬）が少なくとも1頭含まれている確率」と解釈するのが妥当である（もし「実際の1着馬がモデルの上位3頭に含まれる確率（Recall@3）」であれば、90%は驚異的かつ非現実的なほどの超高精度である）。ベンチマーク比較: 16頭立てのレースにおいて、ランダムに3頭選んだ場合に入賞馬が含まれる確率はそれなりに高いが、90%を超えるには強力なフィルタリングが必要である。一般的な競馬ファンの「本命馬（1番人気）」の複勝率（3着内率）は約65%前後である。上位人気3頭をボックス買いした場合の的中率などを考慮すると、90.06%という数値は、商用AIとしてもトップクラスの安定性を示している 。実用性: この精度は、「軸馬選定」において極めて強力である。モデルが選んだ3頭を軸にすれば、9割のレースで軸が外れないことを意味するため、連対系（馬連・3連複）の軸として非常に信頼性が高い。4.2 Top5 $\ge$ 3 (28.23%) の評価これは、「モデルが予測した上位5頭の中に、実際の1着・2着・3着馬がすべて含まれている確率」を指す。つまり、上位5頭の3連複5頭ボックス（10点買い）が的中する確率である。妥当性: 1レースあたりの平均配当や控除率（約25%）を考慮すると、28%の確率で3連複が的中するならば、配当次第ではあるが、十分にプラス収支（回収率100%超）を狙えるラインである。一般的に3連複の平均的中率は数%〜10%程度であり、28%というカバー率は、モデルが「有力馬のグループ（Pool of Contenders）」を極めて正確に特定できていることを示唆する 。4.3 JRA vs NAR の精度差Q5-3に関して、一般的にAIモデルは**JRA（中央競馬）**において高い精度を示す傾向がある。JRAは馬場管理が徹底されており、データの信頼性が高く、レース展開が紛れにくい。一方、**NAR（地方競馬）**はコースの癖が強く（極端な小回り等）、騎手の技量差も激しいため、Global Features偏重のモデルでは精度が低下しやすい。EOI-PLの「Global 95%」という設定は、特にNARにおいて弱点となる可能性が高く、地方競馬への適用にはContext Featuresの重み増強が不可欠である 。5. データ戦略と実装最適化5.1 学習データ量（5年間・6.6万レース）Q6-1, Q6-2に対し、5年分のデータ（約66,668レース）は、モデルの学習には必要十分かつ適切な量である。データの鮮度: 競馬の環境（種牡馬のトレンド、調教技術、馬場の高速化）は年々変化するため、10年前のデータは現在の予測にとってノイズになり得る。直近3〜5年のデータに重みを置く（Time Decay）手法が一般的であり、5年という期間はトレンドの変化を捉えつつ、サンプルサイズを確保する上でバランスが良い 。エントリー数: 67万エントリー（出走履歴）は、Power EPのようなベイズモデルやGBDTを学習させるには十分な規模である。5.2 次世代の実装：Deep LearningとEmbeddingsQ7-1に関連し、特徴量の次元削減や表現学習においては、従来のPCAやt-SNEよりも、**ニューラルネットワークによる埋め込み（Entity Embeddings）**が推奨される。Track2Vec / Horse2Vec: 自然言語処理のWord2Vecのように、各競走馬や競馬場を低次元のベクトル（例：32次元）に変換する。これにより、「中山競馬場」と「阪神競馬場」の類似性や、「ディープインパクト産駒」と「瞬発力勝負」の相性などを、ベクトル空間上の内積として計算できる。EOI-PLの疎（Sparse）なコンテキストデータを、密（Dense）な埋め込みベクトルに置き換えることで、未知の条件に対する汎化性能が劇的に向上する 。6. 商用システムとの比較と競争力6.1 主要競合システムAlphaImpact: 電脳賞（春）優勝チームによる開発。主にRandom ForestやGBDTを使用し、回収率重視の設計。特徴量はJRA-VANデータをフル活用し、指数化している 。Netkeiba (SPAIA等含む): 日本最大級のデータベースを活用した「オリジナルAI」。膨大な過去データと、ユーザーの予想傾向なども加味したアンサンブルモデルであると推測される 。6.2 EOI-PLの強みと弱み強み（Pros）:確率的アプローチ: 単なる点予測ではなく、Power EPによる事後分布（不確実性の幅）を持っているため、「期待値（Expected Value）」の計算が正確である。オッズが美味しい「過小評価された馬」を見つける能力に長けている。ランキング特化: ListMLEにより、3連単や3連複といった「組み合わせ馬券」の最適化において、一般的な回帰モデルよりも有利である。弱み（Cons）:特徴量の硬直性: 95:5という固定比率は、柔軟な現代AI（Deep Learning/GBDT）に比べて適応力に欠ける。非構造化データの不使用: 画像（パドック）、動画（レース映像からのトラッキング）、テキスト（厩舎コメント）を活用できていない可能性がある。7. 戦略的提言とロードマップEOI-PL方式を現在の競争環境においてトップティアに引き上げるための改善案を以下に提示する。短期的な改善（Immediate Wins）動的重み付けの導入: GlobalとContextの比率を固定せず、レース条件（クラス、競馬場、天候）を入力としたメタモデル（例えば軽量なGBDT）によって、各特徴量グループの重みを動的に調整する「Meta-Weighting」機構を導入する。特徴量相互作用の追加: Context-Specificなデータ（トラック適性）とGlobalデータ（脚質、血統）のクロス積（Interaction Terms）を明示的に特徴量として追加し、ListMLEで再学習を行う。中期的な改善（Structural Evolution）Embeddingsへの移行: track_adaptation 辞書を廃止し、競馬場ID、騎手ID、馬IDをLearnable Embeddingsとして実装する。これにより、データ希薄な地方競馬場や新人騎手に対しても、類似性に基づいた推論が可能となる。Power EPのオンライン化: レース結果が出るたびに、バッチ再学習を待たずにPower EPの更新式を用いてリアルタイムにパラメータ（馬の強さ分布）を更新するパイプラインを構築する 。長期的な改善（Paradigm Shift）トラッキングデータの活用: 「Big Data Derby」で示されたように、GPSや画像解析によるフレーム単位の座標データを導入し、LSTMやTransformerで「レース展開そのもの」をシミュレートするモデルへと進化させる 。これにより、単なる統計的予測を超えた、物理シミュレーションに近い精度を実現する。結論EOI-PL方式は、Plackett-Luce + Power EPという極めて強力かつ理論的に洗練されたコアエンジンを持っており、そのランキング予測能力（Top3精度90%超）は現在の市場においても十分に競争力がある。しかし、特徴量管理（95:5配分）の前時代的な硬直性が、そのポテンシャルを制限しているボトルネックである。この「重みの呪縛」を解き、Deep Learning技術による柔軟なコンテキスト適応と表現学習を取り入れることで、EOI-PLは2026年以降の競馬予測AI市場をリードする「次世代ハイブリッドモデル」へと進化し得るだろう。項目EOI-PL 現状評価推奨される進化形 (2026 Standard)モデル構造Plackett-Luce (Generative Ranking)Plackett-Luce + Deep Neural Network (Neural PL)推論エンジンPower EP (Robust Bayesian)Power EP (Online) + Variational Autoencoder特徴量配分静的 (Global 95% / Context 5%)動的 Attention (Context-Aware)データ表現統計値 (Average Rank etc.)分散表現 (Entity Embeddings)学習目的ListMLE (Ranking Optimization)ListMLE + PolyLoss / LambdaLoss以上