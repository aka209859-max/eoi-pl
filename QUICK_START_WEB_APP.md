# 🚀 EOI-PL Webアプリ クイックスタート

**今から作るもの**: 地方競馬予想をボタン1つで表示するWebアプリ

---

## 📋 **Phase 1: Webアプリ実装（開始）**

### **Step 1-1: プロジェクト構造作成**

CEOのWindows PCで以下を実行:

```powershell
# webapp フォルダを作成
cd E:\eoi-pl
mkdir webapp
cd webapp

# Honoプロジェクトを初期化（Cloudflare Pages テンプレート）
npm create -y hono@latest . -- --template cloudflare-pages --install --pm npm
```

---

## 🎯 **次のステップ（順番に実行）**

1. ✅ 計画書作成完了（このファイル）
2. → プロジェクト構造作成（上記コマンド）
3. → Backend API実装
4. → Frontend UI実装
5. → ローカルテスト
6. → Cloudflareデプロイ

---

**準備完了です！次のコマンドを実行してください！** 🏇
