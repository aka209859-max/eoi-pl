{
  "name": "eoi-pl-webapp",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "wrangler pages dev dist",
    "deploy": "npm run build && wrangler pages deploy dist --project-name eoi-pl-webapp"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@hono/vite-cloudflare-pages": "^0.4.2",
    "@cloudflare/workers-types": "^4.20250105.0",
    "vite": "^5.0.0",
    "wrangler": "^3.78.0"
  }
}
